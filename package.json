{
  "name": "employee-app-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build index.js --no-cache --target=default --no-scope-hoist --no-content-hash"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "targets": {
    "default": {
      "context": "node",
      "includeNodeModules": true,
      "sourceMap": false
    }
  },
  "dependencies": {
    "express": "^4.17.1",
    "json-server": "^0.16.3",
    "morgan": "^1.10.0",
    "parcel": "^2.0.0-beta.2"
  },
  "engines": {
    "node": "14.x"
  }
}
